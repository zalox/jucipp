#include "asm.h"

// #include <string>

namespace Asm {
  enum class Token {
    COMMENT,
    EMPTY,
    LABEL
  };
  
  const auto str = [](const char *){
    return "";
  };
  
  class Assembly {
    
  };
};